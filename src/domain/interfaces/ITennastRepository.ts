
export interface ITennastRepository {
  // get(id: string): Promise<Tennats | null>;
  // getAll(): Promise<Tennats[] | null>;
  // create(entity: Tennats): Promise<void>;
  // update(entity: Partial<Tennats>): Promise<void>;
  // delete(id: string): Promise<void>;
}